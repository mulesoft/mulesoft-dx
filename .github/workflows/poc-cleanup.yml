name: POC Cleanup

on:
  delete:

jobs:
  cleanup-poc:
    name: Remove POC from Akamai
    runs-on: ubuntu-latest
    if: github.event.ref_type == 'branch' && startsWith(github.event.ref, 'poc/')
    environment:
      name: poc

    steps:
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${AKAMAI_PRIVATE_SSH_KEY}" > ~/.ssh/akamai_key
          chmod 600 ~/.ssh/akamai_key
          ssh-keygen -lf ~/.ssh/akamai_key > /dev/null && echo "key parses OK"
        env:
          AKAMAI_PRIVATE_SSH_KEY: ${{ secrets.AKAMAI_PRIVATE_SSH_KEY }}

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Derive POC name
        id: poc
        run: |
          raw="${DELETED_REF#poc/}"
          sanitized="$(printf '%s' "$raw" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9]+/-/g; s/^-+//; s/-+$//')"
          if [ -z "$sanitized" ]; then
            echo "Deleted branch '$DELETED_REF' produced an empty POC name after sanitization" >&2
            exit 1
          fi
          echo "name=$sanitized" >> "$GITHUB_OUTPUT"
          echo "Cleaning up POC: $sanitized"
        env:
          DELETED_REF: ${{ github.event.ref }}

      - name: Remove POC directory from Akamai
        run: |
          echo "Removing POC from TEST environment | POC=${POC_NAME}"
          lftp -u "${AKAMAI_USER},${AKAMAI_PASS}" -e "set sftp:connect-program 'ssh -o BatchMode=yes -o StrictHostKeyChecking=no -o IdentitiesOnly=yes -i ~/.ssh/akamai_key'; rm -rf ${AKAMAI_BASE_PATH}/test/pocs/${POC_NAME}; quit" "sftp://${AKAMAI_HOST}" || true
        env:
          AKAMAI_HOST: ${{ secrets.AKAMAI_HOST }}
          AKAMAI_USER: ${{ secrets.AKAMAI_USER }}
          AKAMAI_PASS: ${{ secrets.AKAMAI_PASS }}
          AKAMAI_BASE_PATH: ${{ secrets.AKAMAI_BASE_PATH }}
          POC_NAME: ${{ steps.poc.outputs.name }}
