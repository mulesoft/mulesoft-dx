Bundled native binaries — darwin-arm64
(binaries carry no version string; this file records their source)

mule-ast
  repo:    mule-versionless
  branch:  feature/apikit-listener-mount  (never merged to master)
  commit:  9c6a4219  (Merge branch 'master' into feature/apikit-listener-mount)
  crate:   cli  --bin mule-ast
  built:   2026-09-24 (cargo build --release)
  note:    Master mule-ast (482c1520) cannot decorate the HTTP listener's
           connection model, so an <http:listener-connection> node ships with
           model=None and the runtime rejects the deploy ("connector node
           reached its builder without a decorated model identity"). The
           listener-mount branch adds that decoration. Required for any apikit /
           HTTP-listener app.

mule-server
  repo:    mule-versionless
  branch:  feature/apikit-listener-mount  (never merged to master)
  commit:  9c6a4219  (Merge branch 'master' into feature/apikit-listener-mount)
  crate:   mule_server  --bin mule-server
  built:   2026-09-24 (cargo build --release)
  note:    Built from the same ref as mule-ast + connectors so the AST model
           decoration, the ABI, and the listener mount (/api/* route) all agree.

descriptor-gen
  repo:    mule-versionless
  branch:  fix/dslgen-connection-hyphenize  (off origin/feat/versionless-descriptor-generation @ 76635d44; never merged to master)
  commit:  d9d818bb  (@W-24295259: Hyphenize connection-provider element name in dslgen; PR #196)
  crate:   mule_descriptor_gen  --bin descriptor-gen
  built:   2026-09-24 (cargo build --release)
  note:    Rebuilt to carry the dslgen fix that hyphenizes a connection
           provider's element name (BasicAuth -> basic-auth-connection), so
           the connector-schemas element matches the AST and model decoration
           resolves it. Without this, an <http:basic-auth-connection> node
           deploys with an undecorated model identity and fails to build.

connectors/  (runtime connector libraries, dlopened by mule-server)
  branch:  feature/apikit-listener-mount  (built from the same ref as mule-server so the ABI matches)
  commit:  9c6a4219  (Merge branch 'master' into feature/apikit-listener-mount)
  built:   2026-09-24 (cargo build --release)
  - libmule_connector_http.dylib   (crate mule_connector_http)
  note:    Registers the request connection provider as "request"
           (element <http:request-connection>), matching the app XML and the
           descriptor-gen dsl.json. An older dylib registered
           "basic-auth-connection" and made <http:request-connection> resolve
           to Unresolved ("expected Connection, got Unresolved").

Recorded manually on 2026-09-24. NOTE: mule-ast / mule-server / connectors here
come from the unmerged feature/apikit-listener-mount ref (listener + apikit
support), NOT master — the update-runtime-binaries skill builds those from
master and would regress listener apps. descriptor-gen stays on its own fix
branch (see above); it is format-compatible with this mule-ast (verified
end-to-end: books-inventory-api-demo deploys and GET /api/books returns 200).
